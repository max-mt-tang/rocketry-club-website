if (!window.getLSCName) { window.getLSCName = function(lsc) { console.warn("getLSCName not available, using fallback"); return lsc; }; } if (!window.getClubDisplayCode) { window.getClubDisplayCode = function(club, clubName) { console.warn("getClubDisplayCode not available, using fallback"); return clubName || club; }; } if (!window.getAgeKey) { window.getAgeKey = function(age) { console.warn("getAgeKey not available, using fallback"); if (age <= 10) return "10U"; if (age <= 12) return "11-12"; if (age <= 14) return "13-14"; if (age <= 16) return "15-16"; if (age <= 18) return "17-18"; return "19O"; }; } if (!window.convertGenderCodeToString) { window.convertGenderCodeToString = function(gender) { console.warn("convertGenderCodeToString not available, using fallback"); return gender; }; } function createBestTimeTableHeader(data) { let stdName = ["B", "BB", "A", "AA", "AAA", "AAAA"]; let cutsAge = data.cutsAge ? (window.getAgeFromCutsValue ? window.getAgeFromCutsValue(data.cutsAge) : data.swimmer.age) : data.swimmer.age; let meetStds = window.getMeetStandards ? window.getMeetStandards(cutsAge) : []; let html = [ '<tr class="wt"><th rowspan="2">Course</th><th rowspan="2">Stroke</th><th rowspan="2">Distance</th>', '<th rowspan="2">Best<br>Time</th><th rowspan="2">Event<br>Date</th><th rowspan="2">', window.createPopup("Event<br>Count", "Total Event Count"), '</th><th class="rk" colspan="4" onclick="toggleRankingsCheckbox()" style="cursor: pointer;">Rankings</th>', ]; if (data.swimmer.age < 19) { html.push( `<th colspan="${stdName.length}" class="mt" onclick="toggleMotivationalCheckbox()" style="cursor: pointer;">`, window.createPopup( "Motivational Standards", "USA Swimming 2024-2028 Motivational Standards", ), "</th>", ); } html.push( `<th colspan="${meetStds.length}" class="mc" onclick="toggleMeetCheckbox()" style="cursor: pointer;">Meet Standards</th></tr>`, '<tr class="gy">' ); html.push( '<th class="rk">', window.createPopup(window.getClubDisplayCode(data.swimmer.club, data.swimmer.clubName), data.swimmer.clubName), '</th>', '<th class="rk">', window.createPopup(data.swimmer.lsc, window.getLSCName(data.swimmer.lsc)), '</th>', '<th class="rk">', window.createPopup( data.swimmer.zone ? data.swimmer.zone[0] + "Z" : "?Z", data.swimmer.zone ? data.swimmer.zone + " Zone" : "Unknown Zone" ), '</th>', '<th class="rk">', window.createPopup("US", "USA Swimming"), '</th>' ); if (data.swimmer.age < 19) { for (let std of stdName) { html.push('<th class="mt">', std, "</th>"); } } for (let std of meetStds) { html.push('<th class="mc">', window.createPopup(std.short, std.meet), "</th>"); } html.push("</tr>"); return html.join(""); } async function createBestTimeTable(data, fastRowList, rowInfo) { storeCurrentSwimmerData(data, fastRowList, rowInfo); let idx = data.events.idx; let ageKey = window.getAgeKey(data.swimmer.age); console.log( "Swimmer gender value:", data.swimmer.gender, "type:", typeof data.swimmer.gender, ); let genderStr = window.convertGenderCodeToString(data.swimmer.gender); console.log("Converted gender string:", genderStr); let stdName = ["B", "BB", "A", "AA", "AAA", "AAAA"]; let cutsAge = data.cutsAge ? (window.getAgeFromCutsValue ? window.getAgeFromCutsValue(data.cutsAge) : data.swimmer.age) : data.swimmer.age; let meetStds = window.getMeetStandards ? window.getMeetStandards(cutsAge) : []; let html = [ '<div class="content" style="margin-top: 8px;">', '<div class="center-row" style="margin-top: -5px;">', '<style id="show-rk-style"></style>', window.createCheckbox( "show-rk", "show rankings", true, `window.toggleColumns('.rk', this.checked)`, ), ]; if (data.swimmer.age < 19) { html.push( '<style id="show-mt-style"></style>', window.createCheckbox( "show-mt", "motivation times", true, `window.toggleColumns('.mt', this.checked)`, ), ); } html.push( '<style id="show-mc-style"></style>', window.createCheckbox( "show-mc", "meet cuts", true, `window.toggleColumns('.mc', this.checked)`, ), ); html.push( window.createCheckbox( "show-shadow", "show shadow", false, `window.toggleShadow(this.checked)`, ), ); html.push( '<span style="display:inline-block"><span class="checkbox-wrapper">', '<input type="checkbox" id="show-top3" onchange="window.toggleTop3Highlight(this.checked)" disabled>', '<label for="show-top3" style="color: #999;">show top 3 (loading BC rankings...)</label>', '</span></span>' ); if (data.swimmer.age < 19) { console.log('Swimmer is under 19, motivational times columns will be visible by default'); } else { console.log('Swimmer is 19 or over, no motivational times needed'); } let ageOptions = [ ["Current Age", "current"], ["10 & Under", "10U"], ["11-12", "11-12"], ["13-14", "13-14"], ["15-16", "15-16"], ["17-18", "17-18"], ["19 & Over", "19O"], ]; let defaultAgeValue; if (data.cutsAge) { defaultAgeValue = data.cutsAge; } else { let storedCutsAge = localStorage.getItem("cutsAge"); if (storedCutsAge) { defaultAgeValue = storedCutsAge; } else { let swimmerAge = data.swimmer.age; defaultAgeValue = "current"; if (swimmerAge <= 10) { defaultAgeValue = "10U"; } else if (swimmerAge >= 11 && swimmerAge <= 12) { defaultAgeValue = "11-12"; } else if (swimmerAge >= 13 && swimmerAge <= 14) { defaultAgeValue = "13-14"; } else if (swimmerAge >= 15 && swimmerAge <= 16) { defaultAgeValue = "15-16"; } else if (swimmerAge >= 17 && swimmerAge <= 18) { defaultAgeValue = "17-18"; } else if (swimmerAge >= 19) { defaultAgeValue = "19O"; } } } let cutsAgeSelect = new Select( "cuts-age-select", ageOptions, defaultAgeValue, (value) => { updateCutsForAge(value).catch(error => { console.error("Error updating cuts for age:", error); }); }, ); cutsAgeSelect.class = ""; html.push( '<span style="display:inline-block;margin-left:15px;vertical-align:top;margin-top:-10px;line-height:1;">', '<span style="margin-right:5px;vertical-align:middle;display:inline-block;font-size:13px;">cuts age:</span>', '<span style="vertical-align:middle;display:inline-block;">', cutsAgeSelect.render(true), "</span>", "</span></div>", ); for ( let courseIndex = 0; courseIndex < _courseOrder.length; courseIndex++ ) { let course = _courseOrder[courseIndex]; if (courseIndex > 0) { html.push('<div style="margin-top: 30px;"></div>'); } html.push('<table class="fill top-margin"><tbody>'); let header = createBestTimeTableHeader(data); html.push(header); let courseRowsAdded = false; for (let i = 0; i < fastRowList.length; ++i) { let row = fastRowList[i]; let time = row[idx.time]; let date = row[idx.date]; let event = row[idx.event]; let timeInt = window.timeToInt(time); let eventStr = _eventList[event]; let [dist, stroke, rowCourse] = eventStr.split(" "); if (rowCourse !== course) { continue; } if (rowInfo[i].length == 2 && courseRowsAdded) { html.push(createBestTimeTableHeader(data)); } html.push(`<tr class="d${dist} ${stroke}">`); if (rowInfo[i].length == 2) { html.push( `<td class="age" rowspan="${rowInfo[i][1]}">${course}</td>`, ); } if (rowInfo[i].length > 0) { html.push( `<td class="bold" rowspan="${rowInfo[i][0]}">${_storkeMap[stroke]}</td>`, ); } let count = data.events && data.events.idx ? data.events.filter( (r) => r[data.events.idx.event] == event, ).length : 0; html.push( '<td class="full">', createClickableDiv( dist, `showGraph(null,{pkey:${data.swimmer.pkey},event:${event}})`, ), '</td><td onclick="selectRow(this)" style="cursor: pointer;">', time, "</td><td>", formatDate(date), "</td><td>", count, "</td>", await buildRankingCell( data.swimmer.pkey, timeInt, genderStr, event, ageKey, data.swimmer.zone, data.swimmer.lsc, data.swimmer.club, ), await buildRankingCell( data.swimmer.pkey, timeInt, genderStr, event, ageKey, data.swimmer.zone, data.swimmer.lsc, ), await buildRankingCell( data.swimmer.pkey, timeInt, genderStr, event, ageKey, data.swimmer.zone, ), await buildRankingCell( data.swimmer.pkey, timeInt, genderStr, event, ageKey, ), ); let stds = []; if (data.swimmer.age < 19) { console.log('Processing motivational standards for:', { genderStr, ageKey, eventStr }); if (window.getAgeGroupMotivationTime) { for (let std of stdName) { let stdKey = `${genderStr} ${ageKey} ${eventStr} ${std}`; let result = window.getAgeGroupMotivationTime(stdKey); console.log('Motivational standard lookup:', stdKey, '→', result); stds.push(result); } console.log('Total motivational standards found:', stds.length, stds); } else { console.log('getAgeGroupMotivationTime function not available'); } } let motivationTimeCount = stds.length; for (let std of meetStds) { if (!std || !std[genderStr]) { console.log("Meet standard missing data:", { std: std, genderStr: genderStr, hasGender: std ? !!std[genderStr] : false, }); stds.push(["", 0]); continue; } stds.push(std[genderStr].get(eventStr) || ["", 0]); } let preTime; for (let [i, [stdStr, stdInt]] of stds.entries()) { let css = i < motivationTimeCount ? "mt" : "mc"; if (!stdInt) { html.push(`<td class="${css}"></td>`); continue; } preTime = (preTime && preTime >= stdInt ? preTime : 0) || stdInt * 1.15; let precent = Math.min( 100, Math.max( 0, ((timeInt - stdInt) / (preTime - stdInt)) * 100, ), ); let percent = 100 - (precent < 5 && precent > 0 ? 5 : Math.floor(precent)); let cls = timeInt <= stdInt ? "dp" : "ad"; html.push( `<td class="${css} tc">`, buildTimeCell( stdStr, "", formatDelta(timeInt - stdInt), cls, percent, ), "</td>", ); preTime = stdInt; } html.push("</tr>"); courseRowsAdded = true; } html.push("</tbody></table>"); } html.push("</div>"); return html.join(""); } function createAgeBestTimeTableHeader(uniqueAges) { let html = [ '<tr class="wt"><th>Course</th><th>Stroke</th><th>Distance</th>', ]; for (let age of uniqueAges) { html.push("<th>", age, "</th>"); } html.push("</tr>"); return html.join(""); } function createAgeBestTimeTable(data, fastRowList, rowInfo) { if (!data.events || !data.events.idx) { console.log("createAgeBestTimeTable: events missing idx property"); return '<div class="content"><p>Age best time data not available</p></div>'; } let idx = data.events.idx; let html = [ '<div class="content" style="margin-top: 20px;"><table class="fill"><tbody>', ]; let ages = new Set(data.events.map((e) => e[idx.age])); let uniqueAges = [...ages]; uniqueAges.sort((a, b) => b - a); let header = createAgeBestTimeTableHeader(uniqueAges); html.push(header); for (let i = 0; i < fastRowList.length; ++i) { let row = fastRowList[i]; let event = row[idx.event]; let [dist, stroke, course] = _eventList[event].split(" "); if (rowInfo[i].length == 2 && i > 0) { html.push(header); } html.push(`<tr class="d${dist} ${stroke}">`); if (rowInfo[i].length == 2) { html.push( `<td class="age" rowspan="${rowInfo[i][1]}">${course}</td>`, ); } if (rowInfo[i].length > 0) { html.push( `<td class="bold" rowspan="${rowInfo[i][0]}">${_storkeMap[stroke]}</td>`, ); } html.push( '<td class="full">', createClickableDiv( dist, `showGraph(null,{pkey:${data.swimmer.pkey},event:${event}})`, ), "</td>", ); for (let age of uniqueAges) { let bestTimeEvent = findBestTimeEventByAge(data.events, event, age); if (bestTimeEvent) { let preBestTime = findPreBestTimeByAge(data.events, event, age); let bestTime = bestTimeEvent[idx.time]; let std = formatStandard(bestTimeEvent[idx.std]); let short = formatStandard(std, true); let date = formatDate(bestTimeEvent[idx.date]); let cls = !preBestTime ? "" : window.timeToInt(bestTime) < window.timeToInt(preBestTime) ? "dp" : "ad"; html.push( '<td class="tc">', buildTimeCell(bestTime, window.createPopup(short, std), date, cls), "</td>", ); } else { html.push("<td></td>"); } } html.push("</tr>"); } html.push("</tbody></table></div>"); return html.join(""); } function findBestTimeEventByAge(events, event, age) { if (!events || !events.idx) { console.log("findBestTimeEventByAge: events missing idx property"); return null; } let idx = events.idx; let bestTimeEvent; for (let row of events) { if (row[idx.event] === event && row[idx.age] === age) { if ( !bestTimeEvent || window.timeToInt(row[idx.time]) < window.timeToInt(bestTimeEvent[idx.time]) ) { bestTimeEvent = row; } } } return bestTimeEvent; } function findPreBestTimeByAge(events, event, age) { if (!events || !events.idx) { console.log("findPreBestTimeByAge: events missing idx property"); return null; } let idx = events.idx; let bestTime; for (let row of events) { if (row[idx.event] === event && row[idx.age] < age) { if (!bestTime || window.timeToInt(row[idx.time]) < window.timeToInt(bestTime)) { bestTime = row[idx.time]; } } } return bestTime; } async function createMeetTable(data) { if (!data.events || !data.events.idx) { console.log("createMeetTable: events missing idx property"); return '<div class="content"><p>Meet data not available</p></div>'; } let idx = data.events.idx; let courses = {}; for (let row of data.events) { let course = getEventCourse(row[idx.event]); if (!courses[course]) { courses[course] = []; courses[course].idx = idx; } courses[course].push(row); } let html = ['<div class="content" style="margin-top: 20px;">']; for (let course of _courseOrder) { let evts = courses[course]; if (evts) { html.push( await createMeetTableByCourse( course, evts, data.swimmer.pkey, data.meetDict, ), ); } } html.push("</div>"); return html.join(""); } async function createMeetTableByCourse(course, events, pkey, meetDict) { let idx = events.idx; let html = [ '<div class="match-size"><span>', course, " Event Count: ", events.length, '</span></div><table class="fill"><tbody>', ]; let meetEvents = new Map(); for (let row of events) { let meetEventKey = row[idx.meet] + "-" + row[idx.event]; let evt = meetEvents.get(meetEventKey); if (!evt || window.timeToInt(row[idx.time]) < window.timeToInt(evt[idx.time])) { meetEvents.set(meetEventKey, row); } } let eventList = [...meetEvents.values()]; eventList.sort((a, b) => (a[idx.date] < b[idx.date] ? -1 : 1)); eventList.idx = idx; idx.delta = eventList[0].length; let eventBestTime = new Map(); for (let row of eventList) { let event = row[idx.event]; let timeInt = window.timeToInt(row[idx.time]); let preBestTime = eventBestTime.get(event); if (!preBestTime) { row.push(null); eventBestTime.set(event, timeInt); } else { row.push(timeInt - preBestTime); if (timeInt < preBestTime) { eventBestTime.set(event, timeInt); } } } let columnInfo = countEventTypeByStroke(eventList); let rowInfo = countMeetByAge(eventList, meetDict); let rowCount = 0; for (let rows of rowInfo) { rowCount += rows.length - 1; } html.push( '<tr><th rowspan="2"></th><th rowspan="2">Age</th><th rowspan="2">Date</th>', ); for (let col of columnInfo) { html.push(`<th colspan="${col.length - 1}">${_storkeMap[col[0]]}</th>`); } html.push('<th rowspan="2">Meet</th><th rowspan="2">Team</th></tr><tr>'); for (let col of columnInfo) { for (let i = 1; i < col.length; ++i) { let evt = _eventIndexMap.get(`${col[i]} ${col[0]} ${course}`); let action = `showGraph(null,{pkey:${pkey},event:${evt}})`; html.push( '<td class="full">', createClickableDiv(col[i], action), "</td>", ); } } html.push("</tr>"); let cellInfo = new Map(); for (let row of eventList) { let cellKey = row[idx.meet] + "-" + _eventList[row[idx.event]]; cellInfo[cellKey] = row; } let first = true; for (let rows of rowInfo) { for (let i = 0; i < rows.length; ++i) { html.push("<tr>"); if (first) { first = false; html.push( `<td rowspan="${rowCount}" class="age">${course}</td>`, ); } if (i == 0) { html.push( `<td rowspan="${rows.length - 1}" class="age">${rows[0]}</td>`, ); ++i; } let meetKey = rows[i]; html.push( "<td>", formatDate(meetDict.get(meetKey)[meetDict.idx.date]), "</td>", ); let clubName; for (let cols of columnInfo) { let stroke = cols[0]; for (let j = 1; j < cols.length; ++j) { let dist = cols[j]; html.push(`<td class="d${dist} ${stroke} tc">`); let cellKey = meetKey + "-" + dist + " " + stroke + " " + course; let cell = cellInfo[cellKey]; if (cell) { let delta = cell[idx.delta]; let style = delta > 0 ? "ad" : delta < 0 ? "dp" : ""; let time = cell[idx.time]; let std = formatStandard(cell[idx.std]); let short = formatStandard(std, true); html.push( buildTimeCell( time, window.createPopup(short, std), formatDelta(delta), style, ), ); clubName = await _clubDictinary.loadClubName( cell[idx.lsc], cell[idx.club], ); } html.push("</td>"); } } html.push( '<td class="left">', meetDict.get(meetKey)[meetDict.idx.name], '</td><td class="left">', clubName, "</td></tr>", ); } } html.push("</tbody></table>"); return html.join(""); } function buildTimeCell(time, std, delta, color, percent) { if (!time) { return '<div>&nbsp;</div><div class="st">&nbsp;</div>'; } let html = [`<div class="${color || ""}">${time}</div>`]; if (std) { html.push( `<div class="st">${std}</div><div class="dd ${color || ""}">${delta}</div>`, ); } else { html.push( `<div class="ds ${color || ""}">${delta === "" ? "&nbsp;" : delta}</div>`, ); } if (percent < 100) { html.push(`<div class="r" style="left:${percent}%"></div>`); } return html.join(""); } function countMeetByAge(eventList, meetDict) { if (!eventList || !eventList.idx) { console.log("countMeetByAge: eventList missing idx property"); return []; } if (!meetDict || typeof meetDict.get !== "function") { console.log("countMeetByAge: meetDict is not a valid Map object"); return []; } let idx = eventList.idx; let ages = new Map(); for (let row of eventList) { let age = row[idx.age]; let set = ages.get(age); if (!set) { set = new Set(); ages.set(age, set); } set.add(row[idx.meet]); } let resultArray = []; let ageList = [...ages.keys()]; ageList.sort((a, b) => b - a); for (let age of ageList) { let meetArray = [...ages.get(age)]; meetArray.sort((a, b) => { try { let meetA = meetDict.get(a); let meetB = meetDict.get(b); if ( !meetA || !meetB || !meetDict.idx || !meetA[meetDict.idx.date] || !meetB[meetDict.idx.date] ) { return 0; } return meetA[meetDict.idx.date] > meetB[meetDict.idx.date] ? -1 : 1; } catch (error) { console.log("Error sorting meets:", error); return 0; } }); resultArray.push([age, ...meetArray]); } return resultArray; } function countEventTypeByStroke(eventList) { let idx = eventList.idx; let result = new Map(); for (let row of eventList) { let [dist, stroke, course] = _eventList[row[idx.event]].split(" "); let set = result.get(stroke); if (!set) { set = new Set(); result.set(stroke, set); } set.add(Number(dist)); } let resultArray = []; for (let stroke of _strokeOrder) { let set = result.get(stroke); if (set) { let array = [...set]; array.sort((a, b) => a - b); resultArray.push([stroke, ...array]); } } return resultArray; } function selectRow(clickedCell) { const row = clickedCell.closest("tr"); if (row && row.classList.contains("selected-row")) { row.classList.remove("selected-row"); } else { const allRows = document.querySelectorAll("tr.selected-row"); allRows.forEach((row) => row.classList.remove("selected-row")); if (row) { row.classList.add("selected-row"); } } } function toggleColumns(selector, show) { const elements = document.querySelectorAll(selector); elements.forEach((element) => { if (show) { element.classList.remove("hide"); } else { element.classList.add("hide"); } }); } function toggleShadow(show) { const style = document.createElement("style"); style.id = "shadow-toggle-style"; const existing = document.getElementById("shadow-toggle-style"); if (existing) { existing.remove(); } if (!show) { style.textContent = ` td>.r { background-color: transparent !important; } `; document.head.appendChild(style); } } function initializeShadowToggle() { const checkbox = document.getElementById("show-shadow"); if (checkbox && !checkbox.checked) { toggleShadow(false); } } function toggleRankingsCheckbox() { const checkbox = document.getElementById("show-rk"); if (checkbox) { checkbox.checked = !checkbox.checked; toggleColumns(".rk", checkbox.checked); } } function toggleMotivationalCheckbox() { const checkbox = document.getElementById("show-mt"); if (checkbox) { checkbox.checked = !checkbox.checked; toggleColumns(".mt", checkbox.checked); } } function toggleMeetCheckbox() { const checkbox = document.getElementById("show-mc"); if (checkbox) { checkbox.checked = !checkbox.checked; toggleColumns(".mc", checkbox.checked); } } function toggleTop3Highlight(checked) { document.querySelectorAll(".top3-highlight").forEach((row) => { row.classList.remove("top3-highlight"); }); if (checked) { const bcRankings = []; const allRankingHeaders = document.querySelectorAll('th.rk'); let bcColumnIndex = -1; allRankingHeaders.forEach((header, index) => { console.log('Checking header', index, ':', header.textContent.trim(), header.innerHTML); const headerText = header.textContent.trim(); const bsElement = header.querySelector('.bs'); const bsText = bsElement ? bsElement.textContent.trim() : ''; console.log('Header analysis:', { headerText, bsText, hasBsElement: !!bsElement }); if (headerText === 'BC' || bsText === 'BC' || headerText.includes('BC')) { console.log('Found BC column at header index', index); bcColumnIndex = index; } }); if (bcColumnIndex >= 0) { console.log('BC column found at index', bcColumnIndex); document.querySelectorAll('table.fill').forEach((table, tableIndex) => { console.log(`Scanning table ${tableIndex} for BC rankings`); const rows = table.querySelectorAll('tbody tr'); console.log(`Table ${tableIndex}: Found ${rows.length} rows`); rows.forEach((row, rowIndex) => { const allRankingCells = row.querySelectorAll('td.rk'); console.log(`Table ${tableIndex}, Row ${rowIndex}: found ${allRankingCells.length} ranking cells`); const bcCell = allRankingCells[0]; if (bcCell) { const rankText = bcCell.textContent.trim(); const rankNumber = parseInt(rankText); console.log(`Table ${tableIndex}, Row ${rowIndex}: BC cell text="${rankText}", parsed=${rankNumber}`); if (!isNaN(rankNumber) && rankNumber > 0) { bcRankings.push({ row: row, rank: rankNumber, rowIndex: rowIndex, tableIndex: tableIndex }); console.log(`Added BC ranking: rank ${rankNumber} at table ${tableIndex}, row ${rowIndex}`); } } }); }); } else { console.log('BC column not found in headers'); } console.log('Found BC rankings:', bcRankings.map(r => ({ rank: r.rank, rowIndex: r.rowIndex, tableIndex: r.tableIndex }))); bcRankings.sort((a, b) => a.rank - b.rank); console.log('Sorted BC rankings:', bcRankings.map(r => ({ rank: r.rank, rowIndex: r.rowIndex, tableIndex: r.tableIndex }))); const top3 = bcRankings.slice(0, 3); console.log('Top 3 BC rankings selected:', top3.map(r => ({ rank: r.rank, rowIndex: r.rowIndex, tableIndex: r.tableIndex }))); console.log(`Total BC rankings found: ${bcRankings.length}, selecting top ${Math.min(3, bcRankings.length)} for highlighting`); top3.forEach((ranking) => { ranking.row.classList.add("top3-highlight"); console.log(`Highlighted row ${ranking.rowIndex} with BC rank ${ranking.rank}`); }); if (top3.length > 0) { console.log(`Highlighted ${top3.length} rows with top BC rankings`); } else { console.log('No BC rankings found to highlight'); } } } async function updateCutsForAge(selectedAge) { console.log("updateCutsForAge called with:", selectedAge); localStorage.setItem("cutsAge", selectedAge); await updateMeetCutsInTable(selectedAge); } async function updateMeetCutsInTable(selectedAge) { try { console.log("Updating meet cuts for age:", selectedAge); let currentData = getCurrentSwimmerData(); if (!currentData) { console.error("No current swimmer data available for table regeneration"); return; } console.log("Regenerating table with cuts age:", selectedAge); currentData.cutsAge = selectedAge; let newTableHtml = await createBestTimeTable( currentData, currentData.fastRowList, currentData.rowInfo ); let contentElement = document.querySelector('.content'); if (contentElement) { contentElement.outerHTML = newTableHtml; initializeShadowToggle(); console.log("Table successfully regenerated with new cuts age:", selectedAge); let message = document.createElement("div"); message.style.cssText = "position:fixed;top:10px;right:10px;background:#4caf50;color:white;padding:10px;border-radius:5px;z-index:9999;"; message.textContent = `Meet cuts updated for age: ${selectedAge}`; document.body.appendChild(message); setTimeout(() => { if (message.parentNode) { message.parentNode.removeChild(message); } }, 2000); } else { console.error("Could not find .content element to update"); } } catch (error) { console.error("Error updating meet cuts:", error); } } function storeCurrentSwimmerData(data, fastRowList, rowInfo) { window.currentSwimmerData = { ...data, fastRowList: fastRowList, rowInfo: rowInfo, }; console.log("Stored swimmer data:", window.currentSwimmerData); } function getCurrentSwimmerData() { return window.currentSwimmerData || null; } function getAgeFromCutsValue(cutsValue) { switch (cutsValue) { case "10U": return 10; case "11-12": return 12; case "13-14": return 14; case "15-16": return 16; case "17-18": return 18; case "19O": return 19; case "current": default: return window.currentSwimmerData ? window.currentSwimmerData.swimmer.age : 14; } } window.createBestTimeTable = createBestTimeTable; window.createAgeBestTimeTable = createAgeBestTimeTable; window.createMeetTable = createMeetTable; window.buildTimeCell = buildTimeCell; window.selectRow = selectRow; window.toggleColumns = toggleColumns; window.toggleShadow = toggleShadow; window.initializeShadowToggle = initializeShadowToggle; window.toggleRankingsCheckbox = toggleRankingsCheckbox; window.toggleMotivationalCheckbox = toggleMotivationalCheckbox; window.toggleMeetCheckbox = toggleMeetCheckbox; window.toggleTop3Highlight = toggleTop3Highlight; window.updateCutsForAge = updateCutsForAge; window.getAgeFromCutsValue = getAgeFromCutsValue; window.storeCurrentSwimmerData = storeCurrentSwimmerData; window.getCurrentSwimmerData = getCurrentSwimmerData;