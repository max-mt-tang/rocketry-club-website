async function swimmer(pkey) { console.log("swimmer function called with pkey:", pkey); if (!pkey || pkey === "undefined") { console.log("Invalid pkey, redirecting"); window.location.replace(""); return; } console.log("Loading details for pkey:", pkey); let data = await loadSwimmerDetails(Number(pkey)); console.log("loadDetails returned:", data); await displaySwimmerDetails(data); updateButtonStates(pkey); } async function loadSwimmerDetails(pkey) { console.log("loadDetails called with pkey:", pkey); let data = await LocalCache.func("swimmer/" + pkey, async () => { console.log("Cache miss, loading fresh data for pkey:", pkey); let swimmerCall = loadSwimerInfo(pkey); let eventsCall = loadEvents(pkey); let [swimmerInfo, events] = await Promise.all([ swimmerCall, eventsCall, ]); console.log("swimmerInfo:", swimmerInfo); console.log("events:", events); console.log( "events structure:", events ? { length: events.length, hasIdx: !!events.idx, firstRow: events[0], keys: events.idx ? Object.keys(events.idx) : "no idx", } : "no events", ); if (!swimmerInfo || !events) { console.log( "Missing data - swimmerInfo:", !!swimmerInfo, "events:", !!events, ); window.updateContent("Cannot get data for " + pkey); return; } return { events: events, swimmer: swimmerInfo, }; }); if (data) { console.log("loadDetails: calling postLoadDetails"); return await processSwimmerData(data); } else { console.log("loadDetails: no data returned from cache/API"); } } async function processSwimmerData(data) { if (!data.events || !data.events.idx) { console.log("Events data missing idx property:", data.events); return data; } let idx = data.events.idx; let meets = new Set(data.events.map((e) => e[idx.meet])); while (!window._meetDictinary) { console.log(`🏊 WAITING: _meetDictinary not yet available in processSwimmerData, waiting 100ms...`); await new Promise(resolve => setTimeout(resolve, 100)); } data.meetDict = await _meetDictinary.loadMeets(meets); data.swimmer.birthday = _birthdayDictionary.calculate( data.swimmer.pkey, data.events, data.meetDict, data.swimmer.age, ); if ( !data.swimmer.birthday || !Array.isArray(data.swimmer.birthday) || data.swimmer.birthday.length < 2 ) { console.log( "Failed to calculate birthday for swimmer", data.swimmer.pkey, "using fallback. Original result:", data.swimmer.birthday, ); let currentYear = new Date().getFullYear(); let estimatedBirthYear = currentYear - (data.swimmer.age || 15); data.swimmer.birthday = [ `${estimatedBirthYear}-01-01`, `${estimatedBirthYear}-12-31`, ]; console.log("Fallback birthday set to:", data.swimmer.birthday); } data.swimmer.alias = getAlias( data.swimmer.firstName, data.swimmer.lastName, ); if (data.events.length > 0 && data.events.idx) { let genderValue = data.events[0][data.events.idx.gender]; console.log( "Setting gender from events[0]:", genderValue, "at index:", data.events.idx.gender, ); data.swimmer.gender = genderValue; } else { console.log("No events data available for gender, setting empty"); data.swimmer.gender = ""; } return data; } async function loadEvents(pkey) { let bodyObj = { metadata: [ { title: "time", dim: "[UsasSwimTime.SwimTimeFormatted]", datatype: "text", }, { title: "age", dim: "[UsasSwimTime.AgeAtMeetKey]", datatype: "numeric", }, { title: "std", dim: "[TimeStandard.TimeStandardName]", datatype: "text", }, { title: "lsc", dim: "[OrgUnit.Level3Code]", datatype: "text", }, { title: "club", dim: "[OrgUnit.Level4Code]", datatype: "text", }, { title: "date", dim: "[SeasonCalendar.CalendarDate (Calendar)]", datatype: "datetime", level: "days", sort: "asc", }, { title: "event", dim: "[UsasSwimTime.SwimEventKey]", datatype: "numeric", }, { title: "meet", dim: "[UsasSwimTime.MeetKey]", datatype: "numeric", }, { title: "gender", dim: "[UsasSwimTime.EventCompetitionCategoryKey]", datatype: "numeric", }, { dim: "[UsasSwimTime.PersonKey]", datatype: "numeric", filter: { equals: pkey, }, panel: "scope", }, ], count: 5000, }; let events = await fetchSwimValues(bodyObj, "event"); if (!events) { return; } let idx = events.idx; for (let row of events) { row[idx.date] = row[idx.date].substring(0, 10); } while (!window.timeToInt) { await new Promise(resolve => setTimeout(resolve, 10)); } events.sort((a, b) => a[idx.date] == b[idx.date] ? window.timeToInt(b[idx.time]) - window.timeToInt(a[idx.time]) : a[idx.date] < b[idx.date] ? -1 : 1, ); return events; } async function loadSwimerInfo(pkey) { let bodyObj = { metadata: [ { title: "firstName", dim: "[Persons.FirstAndPreferredName]", datatype: "text", }, { title: "lastName", dim: "[Persons.LastName]", datatype: "text", }, { title: "age", dim: "[Persons.Age]", datatype: "numeric", }, { title: "clubName", dim: "[Persons.ClubName]", datatype: "text", }, { title: "lsc", dim: "[Persons.LscCode]", datatype: "text", }, { title: "pkey", dim: "[Persons.PersonKey]", datatype: "numeric", filter: { equals: pkey, }, }, ], count: 1, }; let values = await fetchSwimValues(bodyObj); if (!values) { return; } let row = values[0]; let swimmer = { firstName: row[values.idx.firstName], lastName: row[values.idx.lastName], age: row[values.idx.age], clubName: row[values.idx.clubName], lsc: row[values.idx.lsc], pkey: row[values.idx.pkey], }; let club = await _clubDictinary.loadClubCode(swimmer.lsc, swimmer.clubName); console.log( "Club lookup result for", swimmer.lsc, swimmer.clubName, ":", club, ); if (!club) { console.log("Club not found, using clubName as fallback"); club = swimmer.clubName; } let zone = window.getLSCZone ? window.getLSCZone(swimmer.lsc) : null; if (swimmer.pkey === 500281 || swimmer.pkey === 1320806) { console.log(`=== CLUB DEBUG for ${swimmer.firstName} ${swimmer.lastName} (${swimmer.pkey}) ===`); console.log('Original club data:', { lsc: swimmer.lsc, clubName: swimmer.clubName, resolvedClub: club, zone: zone }); console.log('Is BC related?', { clubNameIncludesBC: swimmer.clubName?.includes('BC') || swimmer.clubName?.includes('Bellevue'), resolvedClubIncludesBC: club?.includes('BC') || club?.includes('Bellevue'), exactClubName: swimmer.clubName, exactResolvedClub: club }); } swimmer.club = club; swimmer.zone = zone; return swimmer; } async function displaySwimmerDetails(data) { if (!data) { window.updateContent("No swimmer found"); return; } console.log("showDetails called with data:", { hasSwimmer: !!data.swimmer, hasEvents: !!data.events, eventsLength: data.events ? data.events.length : 0, eventsHasIdx: data.events ? !!data.events.idx : false, }); let html = []; html.push(createDetailsPageTitle(data)); if (data.events.length == 0) { console.log("No events found, events length:", data.events.length); html.push("<div>No events found</div>"); window.updateContent(html.join("")); return; } let idx = data.events.idx; if (!idx) { console.log( "Events idx missing, creating idx mapping from known structure", ); console.log("EVENTS DATA SAMPLE - First event:", data.events[0]); idx = { time: 0, age: 1, std: 2, lsc: 3, club: 4, date: 5, event: 6, meet: 7, gender: 8, }; data.events.idx = idx; console.log("Created idx mapping:", idx); } let hide25 = localStorage.getItem("hide25"); if (hide25) { data.events = data.events.filter((e) => e[idx.event] < 80); data.events.idx = idx; } let fastRowList = await getFastRowByEvent(data.events); let rowInfo = new Array(fastRowList.length); let courseCounter = 0; let strokeCounter = 0; for (let i = fastRowList.length - 1; i >= 0; --i) { let info = []; rowInfo[i] = info; ++courseCounter; ++strokeCounter; let [d, s, c] = i == 0 ? ["", "", ""] : _eventList[fastRowList[i - 1][idx.event]].split(" "); let [_, stroke, course] = _eventList[fastRowList[i][idx.event]].split(" "); if (course != c) { info.push(strokeCounter, courseCounter); courseCounter = strokeCounter = 0; } else if (stroke != s) { info.push(strokeCounter); strokeCounter = 0; } } let tabView = new TabView("swimmerTabView"); console.log("Creating tabs..."); let personalBestTable = await createBestTimeTable( data, fastRowList, rowInfo, ); console.log( "Personal Best table created, length:", personalBestTable.length, ); let ageBestTable = createAgeBestTimeTable(data, fastRowList, rowInfo); console.log("Age Best table created, length:", ageBestTable.length); let meetTable = await createMeetTable(data); console.log("Meet table created, length:", meetTable.length); let progressGraph = createProgressGraph(data.swimmer.pkey, data.events); console.log("Progress graph created, length:", progressGraph.length); console.log("Progress graph content preview:", progressGraph.substring(0, 100)); tabView.addTab("<p>Personal Best</p>", personalBestTable); tabView.addTab( createClickableDiv( "Progress Graph", `showGraph(null,{pkey:${data.swimmer.pkey}})`, ), progressGraph, ); tabView.addTab("<p>Age Best</p>", ageBestTable); tabView.addTab("<p>Meets</p>", meetTable); html.push(tabView.render()); html.push(addHide25Botton()); window.updateContent(html.join("")); setTimeout(initializeShadowToggle, 100); } function getAlias(firstName, lastName) { let alias = firstName; if (alias[alias.length - 1] != " ") { if (alias.toLowerCase().endsWith(lastName.toLowerCase())) { alias = alias.substring(0, alias.length - lastName.length); } parts = alias.trim().split(" "); alias = parts.pop(); } else { alias = alias.substring(0, alias.length - 1); } return alias; } function createDetailsPageTitle(data) { let html = []; let displayName; if (data.swimmer.firstName && data.swimmer.lastName) { let firstName = data.swimmer.firstName.trim(); let lastName = data.swimmer.lastName.trim(); let firstWords = firstName.split(" "); let lastWords = lastName.split(" "); let firstNamePart = firstWords[0]; let lastNamePart = lastWords[lastWords.length - 1]; displayName = firstNamePart + " " + lastNamePart; } else { displayName = (data.swimmer.firstName || "") + " " + (data.swimmer.lastName || ""); } html.push( '<div class="match-size header"><span>', displayName, "</span><span>", window.convertGenderCodeToString(data.swimmer.gender), "</span><span>", data.swimmer.age, "</span><span>", data.swimmer.clubName, "</span><span>Birthday: ", (window.BirthdayDictionary ? BirthdayDictionary.format(data.swimmer.birthday) : (data.swimmer.birthday ? data.swimmer.birthday.join(' - ') : '')), "</span><span>Total Event: ", data.events.length, "</span></div>", ); return html.join(""); } function addHide25Botton() { let hide25 = localStorage.getItem("hide25"); return [ '<div style="height:20px;margin:5px"><button onclick="toggle25()" style="position:absolute;right:0">', hide25 ? "Show 25 Events" : "Hide 25 Events", "</button></div>", ].join(""); } function toggle25() { let hide25 = localStorage.getItem("hide25"); if (hide25) { localStorage.removeItem("hide25"); } else { localStorage.setItem("hide25", "1"); } loadContent(); } async function getFastRowByEvent(events) { if (!events || !events.idx) { console.log("getFastRowByEvent: events missing idx property"); return new Map(); } while (!window.timeToInt) { await new Promise(resolve => setTimeout(resolve, 10)); } let fastRowMap = new Map(); for (let row of events) { let eventKey = row[events.idx.event]; let fastRow = fastRowMap.get(eventKey); if ( !fastRow || window.timeToInt(row[events.idx.time]) <= window.timeToInt(fastRow[events.idx.time]) ) { fastRowMap.set(eventKey, row); } } let uniqueEventKeys = [...fastRowMap.keys()]; uniqueEventKeys.sort((a, b) => getEventSortKey(a) - getEventSortKey(b)); let fastRowList = []; for (let key of uniqueEventKeys) { fastRowList.push(fastRowMap.get(key)); } return fastRowList; } async function search(name, all) { if (!name) { window.location.replace(""); return; } let values = await loadSearch(name, all); showSearch(values); } async function searchAll(params) { return await search(params, true); } async function loadSearch(name, all) { let key = "search/" + name + (all ? "<ALL>" : ""); return await LocalCache.func(key, async () => { let values = await loadSwimmerSearch(name, all); if (!values || values.length == 0) { values = await loadClubSearch(name, all); } if (!values || values.length <= 1) { return values; } values = await filterSwimmers(values); if (values) { values.sort((a, b) => a[values.idx.age] - b[values.idx.age]); } return values; }); } async function filterSwimmers(values) { let pkeys = new Set(values.map((v) => v[values.idx.pkey])); let bodyObj = { metadata: [ { title: "pkey", dim: "[UsasSwimTime.PersonKey]", datatype: "numeric", filter: { members: [...pkeys], }, }, ], count: pkeys.size, }; let list = await fetchSwimValues(bodyObj, "event"); if (!list) { return; } pkeys = new Set(list.map((v) => v[list.idx.pkey])); let result = []; let idx = values.idx; result.idx = idx; for (let row of values) { if (pkeys.has(row[idx.pkey])) { result.push(row); } } return result; } function showSearch(values) { console.log("showSearch called with:", values); console.log("values.length:", values?.length); console.log("values.idx:", values?.idx); console.log("First result structure:", values?.length > 0 ? values[0] : 'No results'); if (!values || values.length == 0) { window.updateContent("No result found"); return; } if (values.length == 1) { let pkey = null; if (values.idx && values.idx.pkey !== undefined) { pkey = values[0][values.idx.pkey]; console.log("Using idx mapping for pkey:", pkey); } else { console.log("Search result missing or invalid idx structure:", values.idx); if (values[0]) { pkey = values[0][4] || values[0].pkey || values[0][0]; console.log("Using fallback pkey detection:", pkey); console.log("Available properties in first result:", Object.keys(values[0] || {})); } } if (pkey) { console.log("Navigating to swimmer with pkey:", pkey); window.location.replace("#swimmer/" + encodeURIComponent(pkey)); } else { console.error("Could not determine swimmer pkey from search result"); console.log("Full first result:", values[0]); window.updateContent("Error: Could not determine swimmer ID from search result"); } return; } let html = []; html.push( '<table class="fill top-margin" id="search-table"><tbody><tr class="th"><th></th><th>Name</th><th>Age</th><th>Club</th><th>LSC</th></tr>', ); let index = 0; for (let [name, age, club, lsc, pkey] of values) { html.push( `<tr onclick="go('swimmer', ${pkey})"><td>`, ++index, '</td><td class="left">', name, "</td><td>", age, '</td><td class="left">', club, "</td><td>", lsc, "</td></tr>", ); } html.push("</tbody></table>"); window.updateContent(html.join("")); } async function loadClubSearch(value, all) { let bodyObj = { metadata: [ { title: "name", dim: "[Persons.FullName]", datatype: "text", }, { title: "age", dim: "[Persons.Age]", datatype: "numeric", sort: "asc", }, { title: "clubName", dim: "[Persons.ClubName]", datatype: "text", filter: { contains: value, }, }, { title: "lsc", dim: "[Persons.LscCode]", datatype: "text", }, { title: "pkey", dim: "[Persons.PersonKey]", datatype: "numeric", }, ], count: 5000, }; if (!all) { bodyObj.metadata[1].filter = { to: 18, }; } else { bodyObj.metadata[1].filter = { from: 19, }; } return await fetchSwimValues(bodyObj); } async function loadSwimmerSearch(value, all) { let names = splitNameBySpaces(value); let calls = []; for (let [firstName, lastName] of names) { calls.push( loadSwimmerSearchByFirstAndLastName(firstName, lastName, all), ); } let values = []; let data = await Promise.all(calls); let set = new Set(); let idx = null; for (let vs of data) { if (!vs || vs.length === 0) continue; if (!idx && vs.idx) { idx = vs.idx; } for (let v of vs) { if (!v || !vs.idx) continue; let pkey = v[vs.idx.pkey]; if (!set.has(pkey)) { values.push(v); set.add(pkey); } } } if (idx) { values.idx = idx; } else { console.log("loadSwimmerSearch: No valid idx found, creating fallback"); values.idx = { name: 0, age: 1, clubName: 2, lsc: 3, pkey: 4 }; } return values; } function splitNameBySpaces(input) { const result = []; const words = input.split(" "); for (let i = 0; i <= words.length; i++) { const firstPart = words.slice(0, i).join(" "); const secondPart = words.slice(i).join(" "); result.push([firstPart, secondPart]); } return result; } async function loadSwimmerSearchByFirstAndLastName(firstName, lastName, all) { let bodyObj = { metadata: [ { title: "name", dim: "[Persons.FullName]", datatype: "text", }, { title: "age", dim: "[Persons.Age]", datatype: "numeric", sort: "asc", }, { title: "clubName", dim: "[Persons.ClubName]", datatype: "text", }, { title: "lsc", dim: "[Persons.LscCode]", datatype: "text", }, { title: "pkey", dim: "[Persons.PersonKey]", datatype: "numeric", }, { dim: "[Persons.LastName]", filter: { startsWith: lastName, }, panel: "scope", }, { dim: "[Persons.FirstAndPreferredName]", filter: { contains: firstName, }, panel: "scope", }, ], count: 5000, }; if (!all) { bodyObj.metadata[1].filter = { to: 18, }; } else { bodyObj.metadata[1].filter = { from: 19, }; } return await fetchSwimValues(bodyObj); } console.log("swimmer.js: Exporting functions..."); window.swimmer = swimmer; window.search = search; window.searchAll = searchAll; window.toggle25 = toggle25; console.log("swimmer.js: Functions exported - search:", typeof window.search);